---
# Example only: copy to ansible/vars/secrets.yml (gitignored) if you use local overrides
# without Key Vault, or keep using Key Vault and use this file only for non-secret extras.
#
# Prefer Azure Key Vault in resource group **vault-rg** for all former vault_* secrets;
# see [docs/README.md](../docs/README.md) and [docker/DEPLOYMENT.md](../docker/DEPLOYMENT.md).
#
# Optional local-only variables (examples):
# admin_username: myuser
